"""Operator tools: the mock model endpoint and the room load simulator.

Not part of the running app. Run them with
`uv run python -m euro_chess_studio.tools.<name>` or the Justfile
recipes (`just mock-llm`, `just load-test`).
"""
